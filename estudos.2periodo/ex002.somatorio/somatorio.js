/*
const numero = document.querySelector('input.numero');
const somatorio = document.querySelector('input.somatorio');
const media = document.querySelector('input.media')

function adiciona() {
    let num = parseInt(numero.value);
    if (isNaN(num)){
        alert("Digite um número inteiro!");
        return;
    }

    somatorio += num;
    somatorio.value = res;
    numero.value = "";
    numero.focus();
    
}
*/

const num = document.querySelector('input.numero');
const resultado = document.querySelector('input.somatorio');

